class Manager{
  constructor() {
  }

  determineRoomsAvailable(allRooms) {
    //find date with date.now
    //adjust data to fit the date format the function requires
    //hotel.updateAvailableRooms
  }

  determinePercentFull() {
    // this.determineRoomsAvailable()
    // return hotel.roomsAvailable / hotel.allRooms *100
  }

  determineTodaysRevenue() {
    // this.determineRoomsAvailable()
    // let bookedRooms = hotel.allRooms.filter(room => !hotel.roomsAvailable.includes(room))
    // bookedRooms.reduce((acc, room) => {
    //   acc += room.costPerNight
    //   return acc
    // }, 0)
  }

  getUserReport(){
    // hotel.findUsersBookings(id via input field)
    // instantiate new user (customer), and use the user methods
  }
  
}